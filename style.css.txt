@import url('https://fonts.googleapis.com/css2?family=Bebas+Neue&display=swap'); /* Fuente similar a la del diseño */

:root {
    --primary-orange: #FF8C00; /* Naranja de tu logo */
    --text-white: #FFFFFF;
    --text-black: #000000;
    --card-bg-orange: #FF8C00;
    --card-border: #FFD700; /* Un dorado o naranja más claro para el borde de la carta */
}

body {
    margin: 0;
    font-family: 'Bebas Neue', sans-serif;
    background-color: #000; /* Fondo negro principal */
    color: var(--text-white);
    display: flex;
    justify-content: center;
    align-items: center;
    min-height: 100vh; /* Para que ocupe toda la altura de la pantalla */
    overflow: hidden; /* Evitar scroll si el patrón de fondo es grande */
    position: relative;
}

/* Patrón de fondo con tu logo */
.background-pattern {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-image: url('tuki-burger-logo.png'); /* Usa tu logo aquí */
    background-repeat: repeat; /* Repetir el logo */
    background-size: 150px; /* Tamaño del logo repetido, ajusta si es necesario */
    opacity: 0.1; /* Transparencia para que sea un fondo sutil */
    z-index: 0; /* Asegura que esté detrás de todo el contenido del juego */
}

.game-container {
    background-color: rgba(0, 0, 0, 0.8); /* Un poco transparente para ver el fondo */
    padding: 30px 40px;
    border-radius: 15px;
    text-align: center;
    box-shadow: 0 0 25px rgba(255, 140, 0, 0.7); /* Sombra naranja */
    z-index: 1; /* Asegura que el contenedor del juego esté por encima del fondo */
    max-width: 900px;
    width: 90%;
    position: relative;
}

h1 {
    font-size: 3.5em;
    color: var(--primary-orange);
    margin-bottom: 10px;
    letter-spacing: 2px;
}

.opportunities {
    font-size: 1.5em;
    margin-bottom: 30px;
}

.opportunities span {
    color: var(--primary-orange);
    font-size: 1.2em;
}

.cards-container {
    display: flex;
    justify-content: center;
    gap: 30px; /* Espacio entre las cartas */
    margin-bottom: 30px;
    perspective: 1000px; /* Para la animación 3D de volteo */
}

.card {
    width: 180px; /* Ancho de la carta */
    height: 250px; /* Alto de la carta */
    background-color: var(--card-bg-orange);
    border: 5px solid var(--card-border);
    border-radius: 15px;
    cursor: pointer;
    position: relative;
    transition: transform 0.6s ease;
    box-shadow: 0 5px 15px rgba(0, 0, 0, 0.5);
    transform-style: preserve-3d; /* Permite que los hijos de la carta tengan 3D */
}

.card:hover {
    transform: translateY(-10px) scale(1.03); /* Efecto al pasar el mouse */
}

.card.flipped .card-inner {
    transform: rotateY(180deg);
}

.card-inner {
    width: 100%;
    height: 100%;
    position: relative;
    transition: transform 0.6s;
    transform-style: preserve-3d;
}

.card-face {
    position: absolute;
    width: 100%;
    height: 100%;
    backface-visibility: hidden; /* Oculta la cara trasera */
    display: flex;
    justify-content: center;
    align-items: center;
    border-radius: 10px; /* Bordes redondeados de la cara interna */
    overflow: hidden; /* Para que la imagen no se salga */
}

.card-face img {
    max-width: 80%; /* Logo dentro de la carta */
    max-height: 80%;
    object-fit: contain;
}

.card-back {
    background-color: var(--card-bg-orange);
    transform: rotateY(0deg);
}

.card-front {
    background-color: var(--text-white); /* Fondo de la cara frontal (donde se revela el premio) */
    transform: rotateY(180deg);
    color: var(--text-black); /* Color del texto del frente */
    font-size: 1.8em;
    font-weight: bold;
    flex-direction: column;
    padding: 10px;
    box-sizing: border-box;
}

/* Letras "A" en las esquinas de la carta (como se ve en el diseño) */
.card-face.card-back::before,
.card-face.card-back::after {
    content: 'A';
    position: absolute;
    font-size: 2em;
    font-weight: bold;
    color: rgba(255, 255, 255, 0.7); /* Un color que se vea en el naranja */
}
.card-face.card-back::before {
    top: 10px;
    left: 15px;
}
.card-face.card-back::after {
    bottom: 10px;
    right: 15px;
    transform: rotate(180deg);
}


.instruction {
    font-size: 1.5em;
    margin-bottom: 25px;
    color: var(--text-white);
}

#reset-button {
    background-color: var(--primary-orange);
    color: var(--text-white);
    border: none;
    padding: 15px 30px;
    border-radius: 8px;
    font-family: 'Bebas Neue', sans-serif;
    font-size: 1.8em;
    cursor: pointer;
    transition: background-color 0.3s ease, transform 0.2s ease;
    margin-bottom: 40px;
    letter-spacing: 1px;
    box-shadow: 0 4px 10px rgba(255, 140, 0, 0.5);
}

#reset-button:hover {
    background-color: #FF6600; /* Un naranja un poco más oscuro */
    transform: translateY(-2px);
}

.prize-info {
    position: absolute; /* Para colocarlo en la esquina inferior derecha */
    bottom: 30px;
    right: 30px;
    background-color: rgba(255, 255, 255, 0.1);
    border-radius: 10px;
    padding: 15px 20px;
    text-align: left;
    max-width: 250px;
    box-shadow: 0 0 15px rgba(0, 0, 0, 0.4);
}

.prize-info h2 {
    font-size: 1.8em;
    color: var(--primary-orange);
    margin-top: 0;
    margin-bottom: 10px;
}

.prize-options {
    display: flex;
    flex-direction: column;
    gap: 10px;
}

.prize-item {
    display: flex;
    align-items: center;
    gap: 10px;
    font-size: 1.1em;
    color: var(--text-white);
}

.prize-item img {
    width: 40px;
    height: 40px;
    object-fit: contain;
    background-color: white; /* Fondo blanco para logos */
    border-radius: 5px;
    padding: 5px;
}
.prize-item:last-child img {
    background-color: transparent; /* El logo de Tuki Burger no necesita fondo blanco si ya es blanco */
}


/* Estilos para cuando se revela un premio */
.card-front.winner-coke img {
    width: 60px; /* Tamaño de la Coca Cola en la carta ganadora */
    height: auto;
    margin-bottom: 10px;
}
.card-front.winner-coke::after {
    content: 'COCA-COLA';
    font-size: 1.2em;
    color: var(--text-black);
    margin-top: 5px;
}

.card-front.winner-discount img {
    width: 70px; /* Tamaño del logo de Tuki en la carta ganadora */
    height: auto;
    margin-bottom: 10px;
}
.card-front.winner-discount::after {
    content: '-20% DESC.';
    font-size: 1.2em;
    color: var(--text-black);
    margin-top: 5px;
}

.card-front.loser::after {
    content: '¡FALLASTE!';
    font-size: 1.5em;
    color: #CC0000; /* Rojo para fallar */
    font-weight: bold;
}